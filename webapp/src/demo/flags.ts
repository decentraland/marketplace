// Demo (curated branch): master switch for the curated-marketplace test.
// Lives in its own module so low-level modules (selectors) can read it
// without importing the whole demo dataset.
export const CURATED_MARKETPLACE_ENABLED = true
