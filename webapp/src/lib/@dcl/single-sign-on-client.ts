// This is only used for testing as the single-sign-on-client is an ESM and jest do not support them

export const init = jest.fn()

export const getIdentity = jest.fn()

export const storeIdentity = jest.fn()

export const clearIdentity = jest.fn()
